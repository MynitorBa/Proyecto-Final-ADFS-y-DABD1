Primero:
iex (Get-Content ".\generar-docs-csharp.ps1" -Raw)

Segundo:
docsify serve docs-csharp

Opcional:
# IR A LA CARPETA
cd "C:\Proyecto-Final-ADFS-y-DABD1\Aero Linea\Aerolinea.API\Aerolinea.API.Tests"

# INSTALAR DEPENDENCIAS (si no están)
dotnet restore

# INSTALAR PAQUETES DE TESTING (si no están)
dotnet add package xUnit --version 2.4.2
dotnet add package Moq --version 4.20.70
dotnet add package FluentAssertions --version 6.12.0
dotnet add package Microsoft.Data.SqlClient --version 6.1.4
dotnet add package coverlet.collector --version 6.0.0

# EJECUTAR TESTS
dotnet test

# CON VERBOSE
dotnet test --verbosity detailed

# CON COBERTURA
dotnet test --collect:"XPlat Code Coverage"